# Release Notes

## V16 Bug Fix - 按文件分图修复 (2026-05-06)

### 🐛 修复
- **按文件分图** — 修复了点击后将所有文件所有字段都绘入图表的问题，现在只显示用户已加载/已绘制的数据

---

## V16 (Latest) - 全功能版

**发布日期**: 2026-04-29

### 🎉 新功能
- **曲线加粗** — StrokeThickness 从 1.5 提升到 2.5，提高视觉对比度
- **高对比度配色** — 12色 RGB 配色方案
- **搜索框模糊过滤** — 输入关键字后只显示匹配项，不相关项隐藏
- **图表保存为图片** — 右键图表区 → "保存为图片..."
- **文件拖拽加载** — 从资源管理器拖入 CSV 文件直接加载
- **X轴粒度控制** — 输入框设置 X 轴刻度间隔（0=自动）
- **背景色切换** — 可选 白色/浅灰/黑色/深灰 四种背景主题
- **常用预设** — 保存当前曲线组合为预设，双击一键应用
- **浮动窗口** — 标题栏 ↗ 按钮，将图表弹出到独立窗口
- **网格布局** — 工具栏输入 M行×N列，按网格排列图表
- **选中高亮** — 选中的图表显示蓝色边框和背景
- **基线对比** — 红色竖线贯穿所有图表，交叉点显示数据标签
- **标签防重叠** — Y 值接近时自动错开标签
- **单击显示数据** — 左键点击图表，显示所有曲线在该X位置的 Y 值

### 🔧 改进
- 控制栏布局优化，标签和控件间距加大
- 预设按钮和对话框字体不再被截断
- 基线数据标签直接渲染在图表上（OxyPlot TextAnnotation）

---

## V15 - 合并版

**发布日期**: 2026-04-29

### 🎉 新功能
- 叠图模式 + 多图模式合一，一个工具满足两种需求
- 「全部叠到一图」按钮：一键合并所有图表的曲线到第1个图表
- 「按文件分图」按钮：自动为每个文件创建独立图表
- 图例自动加 `数据1:` `数据2:` 前缀，区分曲线来源
- 选中文件时自动切换到包含该文件曲线的图表
- 图表标题栏显示当前曲线数量

### 🐛 修复
- 目标图表下拉框显示不全（加宽到280px）
- 删除图表后自动补充，确保至少保留1个图表
- 图表ID递增不重复（使用独立计数器）
- 双击删除曲线功能正常工作

### 🔧 改进
- 拖拽操作简化为2段格式，始终路由到活动图表
- 合并后状态提示更明确
- 按文件分图时图表名用简洁的 `数据1/2/3` 格式

---

## V14 - 多图版

**发布日期**: 2026-04-29

### 🎉 新功能
- 多图并排显示，支持添加/删除多个独立图表
- 每个图表有独立的 PlotModel、曲线列表、颜色循环
- 图表选择器切换目标图表
- 每个图表有标题栏和关闭按钮
- 图表自动均分高度，超出时滚动显示

---

## V13 - 叠图版

**发布日期**: 2026-04-29

### 🎉 功能
- CSV 文件加载，GBK/UTF-8 编码自动检测
- 表头配置对话框，可配置表头行号和数据起始行，带预览
- 字段列表，按文件分组，支持搜索过滤
- 曲线绘制，双击或拖拽字段到图表区
- X 轴选择，默认"时戳"，可切换任意字段
- 图表交互，滚轮缩放、右键平移
- 图例显示，底部水平排列
- 文件管理，添加/替换/删除文件

### 🐛 修复
- [A][D][E] 标签不显示问题
- 图例底部截断
- 文件列表第一项顶部截断
- 双击删除曲线功能
